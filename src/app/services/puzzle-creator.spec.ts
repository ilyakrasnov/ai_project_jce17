import {PuzzleCreatorService} from './puzzle-creator';

describe('PuzzleCreatorService', () => {
  let service: PuzzleCreatorService;

  beforeEach(() => {
    service = new PuzzleCreatorService();
  });

  fit('locates groups', () => {

    expect(true)
		.toEqual(false);
  });
});
